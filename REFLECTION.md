# Part 3：反思（Reflection）

这个 Lab 的核心其实是一句话：**coding agent ≠ agent harness**。

同一个模型，Part 1 只能「看代码、猜行为、写测试」；Part 2 给它加上 Playwright（眼睛和手）、Skill（该怎么测的方法论）、再让它从自己的 testing trace 里回收经验（沉淀），能力就完全不同了。而人负责判断这些结果到底对不对、值不值得留下。

```
                Coding Agent
                     │
          ┌──────────┴──────────┐
      Part 1                  Part 2
   Default Agent          Enhanced Harness
          │                     │
      看代码              ┌──────┴──────┐
      写测试          Playwright      Skill
      跑测试           操作真实网页    提供测试方法
                          └──────┬──────┘
                            Testing Trace
                                 ↓
                          改进 Web Skill
                                 ↓
                         更好的后续测试
```

## 1. 和 Part 1 比，Playwright 带来了什么？

Part 1 只有 Vitest + jsdom：假的浏览器、假的 DOM。Agent 看到 `<button onClick={addToCart}>` 就**推测**「点它购物车会 +1」，然后基于这个推测写测试。它证明的是「组件函数被调用了」，不是「用户真的能下单」。

Part 2 用 Playwright 打开真实 Chromium、跑真实 Vite dev server，于是从「基于代码理解写测试」变成「**验证真实的用户行为**」：

- 页面跳转：购物车为空时访问 `/checkout` 会被踢回首页；
- 真实的 localStorage：刷新页面后购物车还在；
- 浏览器自带的表单校验（`type="email"` 会在 JS 校验之前就拦住提交）；
- 定时行为：下单成功 5 秒后自动跳回首页。

这些都是「代码看起来没问题，但页面上真出事」的那一类问题 —— 按钮定位不到、点了状态没更新、错误提示没显示、某一步卡住。光读 source code 发现不了。

一句话：Part 1 测的是代码，Part 2 测的是**用户实际经历的流程**。

## 2. Skill 比「直接写 prompt」多了什么价值？

关键区别是：**Playwright 是工具，Skill 不是工具。**

- Playwright 说的是：「你**可以**操作浏览器。」
- Skill 说的是：「你**应该怎么好好地**测试浏览器。」

`Test the checkout workflow.` 这句 prompt 没有回答任何一个真正决定测试质量的问题：selector 怎么选？要不要先 inspect 页面？什么叫有意义的断言？要不要测错误路径和边界情况？测试挂了该怎么办？能不能为了让测试通过去改应用代码？

所以可以记成：**Prompt = What to do，Skill = How to do it well。**而且 prompt 是一次性的，Skill 是**可复用、还能不断变好的说明书** —— 明天换成测退款、测优惠券，这份 Skill 照样能用。

具体它做了三件事：

1. **把这个项目的坑写下来**：商品有折扣，购物车总价是 `price × (1 - discount/100)` 不是标价；`/auth` 上有两个叫「Sign Up」的按钮。不写下来，下次还要重新踩一遍。
2. **定下规矩**：期望值必须从 `src/data/products.js` 导入，不许照着页面截图抄数字；不许为了让测试通过去改应用代码。
3. **规定「做完」的标准**：连续跑两遍都过、至少做过一次变异检查、发现的 bug 要记录而不是绕过。

## 3. Agent 从自己的测试过程里学到了什么？

第一次跑完之后回头看 trace，真正浪费时间的是这几件事，它们都被写回了 Skill：

- `/auth` 上「Sign Up」有两个（切换按钮 + 提交按钮），Playwright 严格模式直接报错 → 教训：用**容器**来区分（`form` 里的那个），不要用 `.nth(0)` 猜位置。
- 首页每个商品卡片都有「Add to Cart」→ 改成从商品详情页 `/products/:id` 加购，那里只有一个按钮，问题自然消失。**换一个页面，比写一个复杂选择器更好。**
- `type="email"` 的输入框，浏览器会在应用自己的校验之前就拦住 `not-an-email`，导致应用的错误提示根本不出现 → 要用 `user@example` 这种「浏览器放行、应用不放行」的值。
- 结账页的姓名和邮箱是**预填**的，所以测「必填校验」必须先清空，否则测了个寂寞。
- Vitest 的默认匹配规则会把 `e2e/*.spec.js` 也收进去 → 要在配置里排除 `e2e/**`。
- 第一次跑就全绿，其实并不能说明什么。真正让人放心的是**变异检查**：故意把商品数量 3 改成 4，确认测试真的会红，再改回来。

这里要说清楚：这**不是** Agent 像人一样「永久学会了」。模型本身没有变。变的是它下次会读到的那份 Skill —— 把这一轮踩坑中有价值的部分，写进了一个可复用的外部文件。区别在于：只改 `.spec.js`，这个经验只解决了这一次；写进 Skill，下次一开始就知道该怎么做。

## 4. 更新 Skill 之后，工作流变好了吗？

变好了，但不要说得太绝对 —— **主要提升的是 consistency，不是让 Agent 变得完全自主。**

它没有让测试跑得更快，也没让测试变多。它的作用是：**下一次做同类任务时，前面那几个坑不用再踩一遍。**
第一版 Skill 只写了「用 role 和 label 选元素」这种通用建议；更新后写的是「这个项目里，`/auth` 有两个 Sign Up，这样区分」这种具体做法。前者任何人都知道，后者只有踩过才知道。

但 Skill 不是魔法，Agent 照样会犯新的错。准确的说法是：Skill 减少了**重复犯同一个错**，让流程更系统，但人工 review 仍然必要。

更新后重跑整套测试：10 个通过，单元测试也没受影响，应用代码一行没改。

## 5. 哪些地方仍然需要人来判断？

这是最关键的一点。Agent 能做的是 write → run → debug → fix，但它不一定知道「**这到底是不是一个好测试**」。

比如它写 `expect(page.locator('.total')).toBeVisible()`，测试通过了 —— 但这根本没证明总价算得对。你真正想验证的是 `Total = $39.98`。

所以人要判断三件事：

- **测试失败到底是谁的错？** 挂了可能是应用 bug、测试 bug、selector 写错、timing 问题，也可能是 Agent 理解错了需求 —— 绝不能一律「测试红了 → 改应用」。本项目就遇到一个真的：结账页订单明细每行用原价 `price × quantity`，下面的 Total 却是折后价，明细加起来对不上总价。这是应用写错了。是「改测试迁就应用」还是「保留测试、报告 bug」，必须人来决定。我最后用 `test.fail()` 把它钉住：套件保持绿色，但 bug 一直可见，将来谁修好了应用，这个测试会立刻变红提醒。
- **覆盖够不够？** success / failure / edge case / state change 有没有都测到 ——「哪些流程对业务重要」是人的判断。
- **Skill 里哪条经验值得留下？** 这是 general lesson，还是只属于这个 app 的特殊情况？比如某个奇怪的 selector，可能不值得写进通用 Skill。

## 6. 什么时候用 CLI，什么时候用 MCP？

我选了 **Playwright CLI**。

- **MCP 像 agent 的「遥控器」**：打开页面 → 看一眼 → 点一下 → 哦报错了 → 再点另一个。反馈快，特别适合**探索**（「这个 checkout 到底怎么走？」）。但探索完就没了，不留下任何文件。
- **CLI 像 agent 的「测试开发工具」**：写出 `e2e/checkout.spec.js`，跑 `npx playwright test`。仓库里留下的是**能 commit、能 review、能进 CI、能给同事复用的资产**。适合**固化**。

我的取舍：选 CLI 意味着探索变慢 —— 想知道「点这个按钮会怎样」，MCP 直接点就行，CLI 得改测试、跑一遍、看结果。但这个 Lab 的最终交付要求就是 Playwright 端到端测试文件，所以我宁愿探索麻烦一点，也要留下能进仓库的东西。

其实两者不是竞争关系，理想组合是 **MCP for exploration, CLI for codification**：

```
MCP → 探索页面 → 发现 workflow → CLI 写成 .spec.js → commit → CI / regression
```

## 7. 什么时候值得增加 agent harness 的复杂度？

**值得的情况：**

- 同一类任务要做很多次（今天测结账，明天测退款、优惠券）；
- 复杂的 web workflow（登录 → 搜索 → 加购 → 结账 → 支付），需要工具（Playwright）+ 方法论（Skill）+ 持久产出（`.spec.js`）+ 反馈（testing trace）配齐才做得动；
- 项目里有「不看源码就一定会踩」的坑（比如这里的折扣价计算）；
- 产出需要被别人复用（CI、队友、以后的自己）。

**不值得的情况：**

- 一次性小任务（「帮我改个 Python 函数」），套上 Playwright + Skill + MCP 纯属 overkill，配环境的时间比干活还长；
- 项目太简单，默认设置就够了；
- Skill 里只写通用套话（「要写好测试」「注意边界情况」），那还不如不写 —— Skill 的价值全在**具体**。

一句话总结：**harness 的成本是一次性的，收益是每次都有的。** 只要这类任务还会再做第二次，就值得。

---

**整个 Part 3 浓缩成一句话：** Playwright 给 Agent「眼睛和手」，Skill 告诉它「应该怎么测试」，testing trace 让它把这次的经验沉淀下来，而 human 负责判断这些结果到底是不是正确、值不值得保留。
