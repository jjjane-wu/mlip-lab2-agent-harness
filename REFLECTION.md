# Part 3：反思（Reflection）

## 1. 和 Part 1 比，Playwright 带来了什么？

Part 1 只有 Vitest + jsdom：假的浏览器、假的 DOM。它只能证明「组件函数被调用了」，不能证明「用户真的能下单」。

Part 2 用 Playwright 打开了真实的 Chromium，跑真实的 Vite 开发服务器，于是能测到之前根本测不到的东西：

- 页面跳转：购物车为空时访问 `/checkout` 会被踢回首页；
- 真实的 localStorage：刷新页面后购物车还在；
- 浏览器自带的表单校验（`type="email"` 会在 JS 校验之前就拦住提交）；
- 定时行为：下单成功 5 秒后自动跳回首页。

一句话：Part 1 测的是代码，Part 2 测的是**用户实际经历的流程**。

## 2. Skill 比「直接写 prompt」多了什么价值？

Prompt 是一次性的，Skill 是**可以反复使用、还能不断变好的说明书**。

它主要做了三件事：

1. **把这个项目的坑写下来**：比如商品有折扣，购物车总价是 `price × (1 - discount/100)`，不是标价；`/auth` 上有两个叫「Sign Up」的按钮。这些不写下来，下次还要重新踩一遍。
2. **定下规矩**：期望值必须从 `src/data/products.js` 里导入，不许照着页面截图抄数字；不许为了让测试通过去改应用代码。
3. **规定「做完」的标准**：连续跑两遍都过、至少做过一次变异检查、发现的 bug 要记录而不是绕过。

## 3. Agent 从自己的测试过程里学到了什么？

第一次跑完之后回头看，真正浪费时间的是这几件事，它们都被写回了 Skill：

- `/auth` 上「Sign Up」有两个（切换按钮 + 提交按钮），Playwright 严格模式直接报错 → 教训：用**容器**来区分（`form` 里的那个），不要用 `.nth(0)` 猜位置。
- 首页每个商品卡片都有「Add to Cart」→ 改成从商品详情页 `/products/:id` 加购，那里只有一个按钮，问题自然消失。**换一个页面，比写一个复杂选择器更好。**
- `type="email"` 的输入框，浏览器会在应用自己的校验之前就拦住 `not-an-email`，导致应用的错误提示根本不出现 → 要用 `user@example` 这种「浏览器放行、应用不放行」的值。
- 结账页的姓名和邮箱是**预填**的，所以测「必填校验」必须先清空，否则测了个寂寞。
- Vitest 的默认匹配规则会把 `e2e/*.spec.js` 也收进去 → 要在配置里排除 `e2e/**`。
- 第一次跑就全绿，其实并不能说明什么。真正让人放心的是**变异检查**：故意把商品数量 3 改成 4，确认测试真的会红，再改回来。

## 4. 更新 Skill 之后，工作流变好了吗？

变好了，但要说清楚是哪种「好」。

它没有让测试跑得更快，也没让测试变多。它的作用是：**下一次做同类任务时，前面那几个坑不用再踩一遍。**
第一版 Skill 只写了「用 role 和 label 选元素」这种通用建议；更新后写的是「这个项目里，`/auth` 有两个 Sign Up，这样区分」这种具体做法。前者任何人都知道，后者只有踩过才知道。

更新后重跑整套测试：10 个通过，单元测试也没受影响，应用代码一行没改。

## 5. 哪些地方仍然需要人来判断？

这是最关键的一点。Agent 能做的是执行，判断还是得靠人：

- **测试失败到底是谁的错？** 结账页的订单明细里，每一行的价格用的是原价 `price × quantity`，但下面的 Total 却是打过折的价格 —— 明细加起来对不上总价。这是应用真的写错了，不是测试写错了。是「改测试迁就应用」还是「保留测试、报告 bug」，这个决定必须人来做。我最后用 `test.fail()` 把它钉住：套件保持绿色，但 bug 一直可见，将来谁修好了应用，这个测试会立刻变红提醒。
- **测什么、不测什么。** Agent 可以生成很多测试，但「哪些流程对业务重要」是人的判断。
- **测试到底有没有在测东西。** 全绿很可能只是断言写得太松。要不要做变异检查、检查哪一条，是人定的。
- **Skill 里哪条经验值得留下。** 有些是这个项目特有的坑（值得写），有些是偶发噪音（不值得写）。

## 6. 什么时候用 CLI，什么时候用 MCP？

我选了 **Playwright CLI**。

- **CLI**：agent 写出 `.spec.js` 文件，然后跑命令。产出的是**能提交进仓库、CI 和同事都能重跑的资产**。适合最终要沉淀成回归测试的场景。
- **MCP**：agent 通过工具直接操作浏览器（点一下、看一眼、再点一下），反馈快，适合探索页面、复现一个奇怪的 bug。但**探索完就没了**，不会留下任何文件。

我的取舍：选 CLI 意味着探索变慢了 —— 每次想知道「这个页面点了会怎样」，都要跑一次测试，而不是像 MCP 那样问一句就行。但这个 Lab 的最终交付要求就是 Playwright 端到端测试文件，所以留下资产比探索方便更重要。

实际上两者不冲突：**用 MCP 探索，用 CLI 固化**，是比较理想的组合。

## 7. 什么时候值得增加 agent harness 的复杂度？

**值得的情况：**

- 同一类任务要做很多次（今天测结账，明天测退款、优惠券）；
- 项目里有「不看源码就一定会踩」的坑（比如这里的折扣价计算）；
- 产出需要被别人复用（CI、队友、以后的自己）。

**不值得的情况：**

- 一次性的小任务，配环境的时间比干活还长；
- 项目太简单，默认设置就够了；
- Skill 里只写通用套话（「要写好测试」「注意边界情况」），那还不如不写 —— Skill 的价值全在**具体**。

一句话总结：**harness 的成本是一次性的，收益是每次都有的。** 只要这类任务还会再做第二次，就值得。
